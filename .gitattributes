# Normalise line endings: LF in the repository, native in the working copy.
# Keeps Windows checkouts and the Linux build on Vercel byte-identical.
* text=auto eol=lf

# Binary assets — never touch these.
*.webp binary
*.png  binary
*.jpg  binary
*.ico  binary
*.pdf  binary
